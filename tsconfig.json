{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "lib": ["ES2022", "DOM"],
    "types": ["node"],
    "noEmit": true,
    "paths": {
      "@timeclock/core": ["./packages/core/src/index.ts"],
      "@timeclock/hris": ["./packages/hris/src/index.ts"]
    }
  },
  "include": ["packages/**/*.ts", "apps/**/*.ts", "tests/**/*.ts"],
  "//exclude": "WIP Supabase/Postgres persistence port (bs5 fork). Staged in-repo but not yet reconciled with this tree's core API + bundler resolution — see docs/persistence-port.md. Excluded so the known-good build stays green until it's wired into the STORE_DRIVER=postgres driver (ADR 0001).",
  "exclude": [
    "packages/db/**",
    "apps/api/src/env.ts",
    "apps/api/src/worker.ts",
    "apps/api/src/roster-stream.ts",
    "apps/api/src/tokenProvider.ts"
  ]
}
